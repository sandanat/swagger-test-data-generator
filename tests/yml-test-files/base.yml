openapi: 3.0.0

info:
  title: YML test file
  description: test file
  version: 1.0.0

paths:
  /test-method:
    get:
      responses:
        200:
          description: success
          content:
            application/json:
              schema:
                type: number

components:
  schemas:
    Test:
      description: test schema
      type: object
      properties:
        id:
          type: string
          format: uuid
      